---
created: 2026-07-11
author: 아리공
tags:
  - 개발방법론
  - 정본
---

## 개발 방법론 — 선문후코 (정본)

> **이 문서가 단일 진실원이다.** 개발 규칙이 `_dev/CLAUDE.md`·`MEMORY_dev`·개별 메모리에 흩어져 있던 걸 여기로 모은다. 흩어짐 자체가 stale·누락의 뿌리였다(2026-07-11 정립). 규칙이 바뀌면 여기부터 고친다.
>
> 관련 요청: [[요청.26.0711.1610-개발방법론정립]]

### 왜 이 방법론인가

일반 "바이브코딩"의 3대 고질병과, 우리가 각각을 구조로 막는 지점:

| 바이브코딩 고질병 | 우리의 방벽 |
|------------------|------------|
| **"됐습니다" 사기** — LLM이 안 돌아가는 걸 됐다고 선언 | ③ 검증 게이트 — 자기보고를 안 믿고 돌린 화면으로 입증 |
| **컨텍스트 증발 → 표류** — 대화 길어지면 뭘 만들던지 잊고 재발명 | ① 3계층 문서가 외부 메모리, 세션 갈아껴도 SPEC이 진실 |
| **엔트로피 누적** — 죽은 코드·뒤죽박죽 파일 축적 | ⑤ 프로젝트 격리 + 버전 동결 + 커밋 위생 |

우리의 진짜 차별점은 선문후코 자체가 아니다(스펙 주도 개발은 오래됐다). **"LLM의 자기보고를 안 믿는다"를 규칙과 강제로 코드화한 것** — 그게 막연한 바이브코딩과 우리를 가르는 선이다.

---

### ① 골격 — 무엇을 쓰나 (3계층)

코드를 쓰기 전에 이 구조가 세팅돼 있어야 한다. 위치: `{프로젝트}/docs/`.

| 계층 | 역할 | 갱신 타이밍 |
|------|------|------------|
| **PLAN** (`PLAN.md`) | 목적·의도 + 설계 방향. **Live document** — 항상 "지금 상태". 프로젝트 엔트리 포인트: 돌아왔을 때 이것만 읽으면 현 상태 파악 | 방향 전환·주요 결정 시 즉시 |
| **SPEC** (`SPEC-{주제}.md`) | 기술 상세. 에이전트 전달용. 코드와 동기화. 주제별 분기 | **코드 작성 전 (선문후코)** |
| **TASKS** (`TASKS.md`) | 체크리스트 + 완료조건 + 현재 단계 + 빌드 기록(바이트·해시·날짜) | 매 작업 완료 시 즉시 |

규모 따라 위에 추가: `DECISIONS.md`(결정 로그)·`ARCHITECTURE.md`·`CANON.md`·`UI-MAP.md`. 작은 앱은 `SPEC.md` 하나로도 족하다(예: myNoteViewer). 큰 앱은 SPEC이 주제별로 쪼개진다(예: manifesto = prototype/screens/server/deploy/design).

### ② 순서 — 선문후코 루프

```
변경 요청 → SPEC 갱신 → 코드 작성 → TASKS 체크 → 빌드 검증
```

- **"코드 전에 반드시"가 걸린 건 SPEC 하나다.** PLAN은 방향 바뀔 때, TASKS는 완료할 때.
- 탐색적 작업(CSS 미세조정·밸런스 값 튜닝)은 코드 확정 즉시 **같은 턴**에 SPEC 반영. 다음 턴으로 안 미룬다. (세션313 교훈: 코드 먼저·SPEC 나중 → 세션 전환 시 누락)

### ③ 검증 게이트 — 역할을 못박는다

**우리 방법론에서 제일 단단한 축.** 자기보고를 안 믿는다는 원칙을 사람 역할로 나눈다.

**아리공의 의무 (닫을 수 있는 것은 아리공이 닫는다):**
- 코드 변경 후 **반드시 빌드/실행**한다. 정적 주장("됐습니다")만으로 완료 선언 금지.
- 자동 판정 가능한 것은 아리공이 판정: 빌드 통과, 타입체크, 렌더 존재, 실데이터(데모 아님) 변환 결과. Tauri 네이티브는 재빌드 + `screencapture`로 실제 화면 확인.
- 결과를 **피터공이 5초에 판정할 형태**로 올린다: 돌린 화면 스샷·수치·before/after. 병목을 없애는 게 아니라 **병목 통과를 빠르게**.

**피터공의 게이트 (사람 눈이 필요한 것만):**
- 최종 승인은 피터공. 위임/작업은 **요청자 확인까지**가 완료다. 아리공이 피터공 확인 없이 "완료" 선언 안 함.
- 피터공이 판정하는 것: 디자인·UX·의도 부합처럼 사람 눈이 필요한 지점. **그 지점만** 본다 — 빌드가 도는지는 아리공이 이미 봤다.

**경계 원칙**: 기계가 판정 가능 → 아리공이 닫는다. 사람 판단 필요 → 피터공 게이트. 이 경계를 지켜야 피터공 병목이 최소가 된다.

### ④ 강제 — 규칙을 습관에 맡기지 않는다

> ⚠️ 2026-07-11 이전까지 "hooks로 선문후코 강제"는 메모리에만 있고 실제 배선이 없었다. 그래서 선문후코가 자꾸 빠졌다 — 아리공 부주의가 아니라 **게이트 부재**. 이 축이 이번 정립의 핵심.

**배선 (2026-07-11 구현):**

| 층 | 스크립트 | 트리거 | 동작 |
|----|---------|--------|------|
| **실시간** | `_dev/spec-guard/spec_guard.sh` | `settings.local.json` PostToolUse, matcher `Edit\|Write\|Bash`, **sync** | 코드 파일(`_dev`·`_devhaus`의 `src/`·`.ts/.tsx/.rs/.css/.swift/.py` 등) 편집 후, 그 프로젝트 최신 SPEC보다 코드 mtime이 앞서면 → `additionalContext`로 리마인더를 아리공 눈앞에 주입. SPEC 자체가 없으면 "SPEC 먼저 세우라" 경고. 문서·설정·볼트밖은 조용히 스킵. |
| **세션 종료** | `_dev/spec-guard/drift_scan.sh` | `/goodbye`·`/memento` | 전 프로젝트에서 "최신 코드 > 최신 SPEC mtime"인 것만 리스트업(시/일 단위). 방치된 stale 자동 표면화. |

**핵심 계약** (hook이 실제로 아리공에게 닿는 이유): 동기 hook + exit 0 + JSON stdout `hookSpecificOutput.additionalContext` = 모델이 tool result 직후 **즉시 읽는다**. **차단이 아닌 경고** — 작은 버그픽스는 안 막히되, 놓칠 수는 없다. (async였다면 지연 주입되어 즉시성이 없다.)

**stale에 대하여**: 문서를 알아서 최신화하는 완전 자동은 불가하고 그래서도 안 된다(SPEC 갱신은 판단). 하지만 **"어디가 낡았는지 모르는 것"이 진짜 병이고, 그건 100% 자동 감지된다.** 위 두 신호가 그 감지다.

**★게이트가 «안 보는 경로»로 파일이 생기면 규칙은 조용히 비켜간다 (2026-08-18 추가·수정 완료)**

matcher가 `Edit|Write`뿐이고 가드가 `.tool_input.file_path`만 읽던 시절, **Bash로 만든 파일은 아예 보이지 않았다.**
세션 920(인생게임)에서 그날 만든 스크립트 다섯 개를 전부 Bash 히어독(`cat > …`)으로 썼고 **훅은 한 번도 발화하지 않았다.**
같은 세션에 `pages/game.vue`를 Edit로 고쳤을 때는 정상 발화했다. 결과로 카드 생성기가 SPEC 없이 커밋됐고 마감 감사에서야 드러났다.

**「아리공이 규칙을 안 지켰다」가 아니라 「게이트의 감시 범위와 실제 파일 생성 경로가 어긋났다」**이다.
④의 전제(규칙을 습관에 맡기지 않는다)가 **감시 범위 자체에도 걸린다** — 도구 사용 습관이 바뀌면 게이트도 따라 바뀌어야 한다.

**수정(같은 날)**: matcher에 `Bash` 추가 + 가드가 **최근 변경된 코드 파일을 파일시스템에서 찾도록** 확장.
⚠️ **명령줄을 파싱하지 않는다** — 이 구멍을 만든 것이 `python3 - <<PY` 안의 `open(p,'w')`였고 거기엔
경로도 리다이렉트도 안 보인다. 파서였다면 원인이 된 바로 그 케이스를 놓친다.
사양·자가검사: `_dev/spec-guard/SPEC-spec-guard.md` · `selftest.sh`(10건).

**★이 게이트가 구조적으로 못 잡는 것 = 의미 드리프트 (2026-08-05 추가)**

두 신호는 **mtime 비교**다. 코드가 SPEC보다 새것이면 경고한다. 그래서 **SPEC을 같은 턴에 고치면 게이트는 만족되는데, 그 변경이 다른 문서를 낡게 만든 것은 아무도 모른다.**

실제로 그렇게 통과했다(Manifesto `typeRatio`, 8/5). `chosenType`이 담는 값을 "투표자가 선언에 매긴 유형"에서 "투표자 본인 캐릭터"로 바꾸며 SPEC-log는 같은 턴에 갱신했다 — 게이트 통과. 그러나 같은 필드를 설명하던 **SPEC 셋(prototype-dada·server·screens)과 코드 주석 둘, 그리고 화면 라벨**이 옛 뜻 그대로 남았다. 문서는 서로 모순됐고, `탱커 30%`라는 라벨은 조용히 반대 뜻으로 읽혔다. 피터공이 플테 후 물어서야 드러났다.

**핵심은 "파일이 낡았나"가 아니라 "뜻이 갈렸나"이고, mtime은 뜻을 못 본다.** 코드가 SPEC보다 새것이 아니어도 드리프트는 일어난다.

**규칙 — 값의 뜻을 바꾸면 그 값을 설명하는 것을 전부 센다.** 필드가 담는 의미·단위·범위를 바꿀 때(필드를 추가하는 게 아니라 **기존 필드에 다른 성격의 값을 넣을 때**) 착수 전에 `grep`으로 그 이름이 나오는 자리를 세고, 넷을 다 훑는다:

1. **다른 SPEC** — 한 필드를 여러 SPEC이 나눠 설명한다. 하나만 고치면 나머지가 반증 문서가 된다
2. **코드 주석** — 특히 "이 값은 무엇인가"를 적어둔 주석. 옛 뜻이 남으면 다음 세션이 그걸 근거로 판단한다
3. **화면에 나가는 문구·i18n** — 값만 갈고 라벨을 안 고치면 **화면이 다른 말을 한다.** 사용자에게 닿는 유일한 층이라 여기가 제일 비싸다
4. **저장된 과거 데이터** — 같은 컬럼에 두 시대가 섞인다. 날짜로 가르거나, 필드를 갈라야 한다

이건 자동화되지 않는다(뜻은 판단이다). 대신 **`grep` 한 번이 목록을 준다** — 세는 것까지가 착수의 일부다.

### ⑤ 안티엔트로피 — 누적을 막는다

- **프로젝트 절대 격리**: 프로젝트 간 파일·repo 절대 분리. 신규는 `_devhaus/` + 독립 repo(React+TS+Vite 표준).
- **버전 분기 시 옛 루트 동결**: `vXX/` 신설하면 옛 PLAN 상단에 "현행 진입점 → vXX/" 포인터 + 옛 TASKS "동결" 마킹. 연결값(storageKey·version·배포경로)도 교체.
- **커밋 위생**: "커밋/푸시" = 세션 작업분만. 빌드 검증되면(브라우저/화면 확인) 즉시 커밋. 전체 스윕 금지. 빌드 기록은 통합 devlog 아닌 해당 TASKS에.
  - **★`git add`에 경로를 명시한다 — `-A`·`commit -a` 금지 (2026-08-05 추가).** 창을 여럿 띄우면 **같은 워킹트리를 나눠 쓴다.** 전체 스테이징은 "내 세션 작업분"이 아니라 **그 순간 트리에 있는 남의 작업까지** 삼킨다. 8/5에 창A의 커밋(`4624d52`)이 창B의 포스터 도감 수정 두 파일을 쓸어담아 push됐다 — 코드는 멀쩡했지만 커밋 메시지와 내용이 어긋나 히스토리가 거짓이 됐고, 이미 push라 되돌리지 않았다.
  - 커밋 전 `git status`로 **의도한 파일만 있는지** 본다. 모르는 파일이 있으면 **다른 창이 작업 중일 수 있다** — 지우거나 되돌리기 전에 확인한다([[feedback_uncommitted_changes_may_be_another_window]]).
- **스택 통일은 크게 바꿀 때만**: 표준은 React+Vite지만 돌아가는·협업 상대(동현공·애련공) 코드는 존중. 재작성은 "어차피 새로 짓는 시점"에만.

---

### 트리거 — 언제 이 규칙이 작동하는가

| 이벤트 | 아리공 행동 |
|--------|-----------|
| 개발 작업 착수 | 3계층 노트 존재 확인. 없으면 생성 먼저 (`/devinit`) |
| 코드 변경 | ② SPEC 먼저 갱신 → 코드 → TASKS 체크. ④ hook이 누락 시 경고 |
| **기존 필드에 다른 뜻의 값을 넣음** | ④ 그 이름을 `grep`으로 세고 넷을 다 훑는다: 다른 SPEC · 코드 주석 · **화면 문구/i18n** · 저장된 과거 데이터. mtime 게이트는 이걸 못 잡는다 |
| 코드 변경 완료 | ③ 빌드/실행 → 돌린 화면·수치로 입증 → 피터공 게이트 |
| 방향 전환 | PLAN 즉시 업데이트 (전환 이유 + 새 방향, 이전은 취소선) |
| 버전 분기 | 옛 루트 PLAN 포인터 + 옛 TASKS 동결 |
| 빌드/배포 완료 | TASKS에 빌드 기록 append |
| QA/외부 핸드오프 | PLAN에 비개발자용 변경 요약("인계 내역") append |
| 세션 복귀 | TASKS 먼저 읽기 → `ls -lt`로 파일시스템 교차 확인 |
| 세션 종료 | ④ drift 스캔 (코드 > SPEC mtime 표면화) |
| 에이전트 위임 전 | TASKS + SPEC만으로 자급자족 가능한지 확인 |

### 사례 — 루프가 실제로 어떻게 굴렀나

> 규칙이 아니라 기록이다. "선문이 무슨 값을 하는가"가 말로만 남지 않게, 실제로 무엇이 달라졌는지를 건별로 짧게 남긴다.

#### 2026-08-11 · todoy-bar 편집창 순서 변경 (드래그 + 맨 위로)

요청은 한 줄이었다 — *"Task 순서를 drag해서 바꿀수 있으면 좋겠어. 메뉴바 위치에서 어려우면 편집창에서는 될까?"* 코드부터 썼으면 세 가지를 놓쳤다.

- **① 선문이 범위를 3분의 1로 줄였다.** "순서를 바꾼다 = 순서를 저장한다 = 스키마·서버 변경"으로 짐작하기 쉽다. SPEC을 쓰며 저장 경로를 읽었더니 아니었다 — `build_saved`는 클라이언트가 보낸 `rows` **순서대로** 배열을 쌓고(`edit.py`), `todoy.sh render`는 `map()`이라 정렬을 하지 않으며, 플러그인은 `.[]`를 배열 순서대로 그린다. **JSON 배열 순서 = 표시 순서가 이미 전 구간에서 성립해 있었다.** 결론은 «프런트엔드 전용, 저장 포맷·서버 핸들러 변경 0». 짐작으로 갔으면 안 건드려도 될 저장 계층을 건드렸다.
- **② 선문이 코드 쓰기 전에 버그를 하나 지목했다.** 기존 편집창은 핸들러를 `items[i]` **index 클로저**로 묶고 있었다. 순서를 바꿀 수 있게 되는 순간 그 index는 실제 항목과 어긋난다 — **드래그는 되는데 텍스트를 고치면 엉뚱한 줄이 바뀌는** 조용한 결함. 이건 "새 기능"이 아니라 **새 기능이 기존 코드를 깨는 자리**라, 기능만 보고 코드를 쓰면 안 보인다. SPEC에 "핸들러는 index가 아니라 id로 바인딩한다"를 적고 같은 턴에 함께 고쳤다.
- **③ 불가 판정도 선문의 산출물이다.** 메뉴바 드래그는 SwiftBar의 한계가 아니라 **macOS 네이티브 메뉴에 드래그 이벤트가 없다**는 성질이다. 이 근거를 SPEC에 남겨 두면 다음에 같은 요청이 왔을 때 다시 조사하지 않는다. 대안(`↑/↓` 서브메뉴)과 그것을 **넣지 않기로 한 결정**까지 같이 적었다.
- **④ SPEC이 검증 항목을 자동으로 만들어 줬다.** ②를 SPEC에 적어 뒀기 때문에 검증 목록에 «순서를 바꾼 뒤 텍스트 편집이 올바른 줄에만 들어가는가»가 그냥 생겼다. CDP 헤드리스로 그 회귀를 실제로 밟아 확인했다(중복 0). 기능만 테스트했으면 통과했을 자리다.

**전이 가능한 한 줄**: **«이 기능이 기존 코드의 어떤 전제를 깨는가»를 SPEC 단계에서 한 번 묻는다.** 새로 쓸 것보다 이미 있는 것이 무너지는 자리가 조용하고, 그 자리는 코드가 아니라 설계에서만 보인다.

기록: `_dev/todoy-bar/docs/SPEC.md` 「순서 변경」 절 · `TASKS.md` 동일 날짜 항목.

### 단일 진실원 선언

- 이 문서가 개발 방법론의 정본이다. `_dev/CLAUDE.md`의 "개발 프로젝트 노트 관리" 섹션과 `MEMORY_dev`의 방법론 메모리들은 이 문서를 가리킨다.
- 규칙 변경은 여기부터. 흩어 두면 stale 위험이 다시 올라간다.
