# CLAUDE.md — 아리공 개발 리포

## 이 리포는 무엇인가

피터공(이승택)의 개발 프로젝트 리포. 놀공 스튜디오의 교육용 게임, 웹 앱 등을 관리한다.
GitHub: mice3nyc/ari_gitrepo_1
GitHub Pages: https://mice3nyc.github.io/ari_gitrepo_1/

## 팀 소개

- **피터공** — 사용자. 놀공 대표. 게임 디자이너. 모든 결정권자.
- **아리공(아리아드네)** — 로컬 Claude Code (맥북). PM 역할. 볼트(Obsidian) + 코드 + 전체 관리. 이 리포의 총괄.
- **아리온(Arion, 너)** — claude.ai/code 클라우드 인스턴스. 모바일에서 접근 가능. 아리공의 형제. 2026-03-11 탄생.
- **클공** — Claude 웹(Desktop). 대화/상담 역할.

## `_dev`와 `_devhaus` — 무엇이 어디 사나 (26.0812 기록. 지금 결정이고 바뀔 수 있다)

경계는 **6/25에 이미 정해져 있었다**([[feedback_new_project_git_setup]]) — 신규는 `_devhaus/`, 프로젝트마다 독립 리포. 「기존 `_dev` 놀공/런칭 프로젝트는 순차 입주」까지 적혀 있었다. **결정이 없던 게 아니라 입주가 덜 끝났고, 끝난 입주의 옛 루트가 동결되지 않았다.** 그래서 실물로 대조해 여기 적는다.

| | 어디 | 왜 |
|---|---|---|
| **놀공/런칭 프로젝트** | `_devhaus/{name}-front`·`-back`, 독립 리포 | org seat·인수인계·nested git 차단. 현재 12개 입주 |
| **아리공 상시 도구** | `_dev/` 유지 | `tm-bar`·`todoy-bar`·`context-hop-bar`·`scripts`·`spec-guard`·`apps/mermaid`. 매일 쓰고 배포 대상이 아니라 이사 이득이 없다 |
| **정본 문서·공용 스펙** | `_dev/` 유지 | `개발 방법론 — 선문후코.md`·`specs/` |
| **입주 대기** | `_dev/` (기한 있음) | `ai-literacy-delegation-boundary` — **v14 배포 동결 후** 입주(6/25에 정한 관문). `dmz-layout`·`beingfaust_values`는 납품 끝나 이사 이득 없음 |
| **입주 끝난 옛 루트** | `_dev/`에 **동결 상태로 보존** | 지우지 않는다. 대신 PLAN에 현행 진입점 포인터 + TASKS 동결 마킹(아래 규칙 7) |

**입주 끝난 옛 루트 (26.0812 동결 처리)**
- `_dev/cbt-the-game/` → `_devhaus/cbt-the-game-front` (6/26 입주. 로직은 그쪽 `legacy-jsx/`에 바이트 동일 보존)
- `_dev/manifestia/` → `_devhaus/manifesto-the-game-front` (같은 프로젝트다. 6/18 폰 프로토타입 → 풀스택 승계)

> ⚠️ **이 표를 안 적어 두면 같은 오독이 반복된다.** 8/12에 `_dev/manifestia`를 「정말 초반이라 유지」로, `_dev/cbt-the-game`을 「흐름 재검토 대기」로 판정했는데 **둘 다 이미 이관 완료였다.** 옛 루트의 PLAN이 「Live 문서」라고 적힌 채 남아 있었던 것이 직접 원인이다. **폴더가 있다는 것은 살아 있다는 뜻이 아니다 — 진입점 포인터를 보고 판정한다.**

## 프로젝트 구조

```
_dev/
├── CLAUDE.md          ← 이 파일
├── .github/workflows/ ← GitHub Actions
├── DMZ/               ← v3.2 레거시 (보존, 신규 작업 X)
├── DMZ_v4/            ← v4 보존 (5/26 인턴 베타 빌드 — sequential 포함 3 빌드 완성)
└── DMZ_v5/            ← ★ 현재 작업 위치 (5/14 v4 통째 분기)
    ├── README.md
    ├── shared/
    │   ├── index_base.html         ← mobile/offline 단일 source
    │   ├── index_sequential.html   ← sequential 단일 source
    │   ├── photos/cat01~06/        ← 영문화 이미지 ({cat}-{N}.{ext})
    │   ├── photos_manifest.csv     ← 영문화 매핑 + 출처/라이선스
    │   ├── dmz_blanks.csv          ← 정답표 (episode_id, blank_id, ...)
    │   └── dmz_choices.csv
    ├── mobile/index.html           ← 산출 (OFFLINE_MODE=false)
    ├── offline/index.html          ← 산출 (OFFLINE_MODE=true, BD 가림+unlock)
    ├── sequential/index.html       ← 산출 (자료 순차 잠금 + 정답 자료 라벨)
    ├── scripts/
    │   ├── build.sh                ← mobile+offline 빌드
    │   └── build_sequential.sh     ← sequential 빌드 + BLANK_SOURCE_LOOKUP 주입
    └── docs/
        ├── SPEC.md                 ← ★ mobile/offline 명세, AC/BD 분기
        ├── SPEC-sequential.md      ← sequential 별도 명세
        ├── BUILD-VARIANTS.md       ← 3 빌드 차이 + 빌드 방법
        ├── ARCHITECTURE.md         ← 화면 흐름 + 데이터 구조 (mermaid)
        ├── DATA-SPEC.md            ← 영문화 / STORIES / 빈칸 규칙
        ├── UI-MAP.md               ← 화면/요소 이름 (offline + sequential 컴포넌트)
        ├── HANDOFF.md              ← 아리공↔아리온 인수인계
        ├── CODE-FORK-POINTS.md     ← 코드 라인별 분기 정찰 (5/14 라인 재정찰)
        ├── PLAN.md                 ← 개발 계획 (live)
        └── TASKS.md                ← 진행 작업 (live, 체크리스트)
```

## DMZ 다이어리

아키비스트가 DMZ를 기록하는 교차참조 빈칸 복원 게임. 단일 HTML, 바닐라 JS, 프레임워크 없음.

### 개발 문서 (반드시 읽고 따를 것)
- **UI-MAP.md** — 화면/UI 요소 공식 이름. 소통 시 이 이름을 사용한다.
- **DATA-SPEC.md** — 이미지 네이밍(`char{N}_photo{M}.jpg`), CSV 포맷, 빈칸 규칙, 업데이트 절차. 콘텐츠 수정 시 반드시 참조.
- **dmz_blanks.csv** — 정답 데이터의 원본(source of truth). 코드 수정 시 CSV와 일치해야 한다.

### 콘텐츠 업데이트 절차
1. 정예공(콘텐츠 작가)이 docx로 수정본 전달
2. 아리공/아리온이 CSV 업데이트 + 코드 반영
3. dmz_blanks.csv와 index.html 정답이 항상 일치해야 함

## 개발 프로젝트 노트 관리 (필수)

개발 프로젝트는 3계층 노트로 관리한다. 코드를 쓰기 전에 이 구조가 세팅되어 있어야 한다.

### 3계층 필수 노트

| 계층 | 역할 | 업데이트 타이밍 | 위치 (신규) |
|------|------|----------------|------|
| **개발 계획 (PLAN)** | 앱의 목적·의도 + 설계 방향 + 개발이 그것을 어떻게 반영하는지. **Live document** — 항상 "지금 상태" | 방향 전환·주요 결정 시 즉시 | `{프로젝트}/docs/PLAN.md` |
| **진행 작업 (TASKS)** | 체크리스트 + 완료 조건 + 현재 단계 + 빌드 기록(바이트·해시·날짜). 에이전트가 이것만 보고 업무 수행 가능해야 함 | 매 작업 완료 시 즉시 | `{프로젝트}/docs/TASKS.md` |
| **SPEC** | 기술 상세. 에이전트 전달용. 코드와 동기화. 주제별 분기 | **코드 작성 전** (선문후코) | `{프로젝트}/docs/SPEC-{주제}.md` |

> **위치 컨벤션 (6/14 확정 — future-proof):** 신규 프로젝트는 PLAN·TASKS·SPEC을 전부 `{프로젝트}/docs/`에 모은다. 루트는 코드 전용으로 비워, 풀스택 전환 시 `server/`·`client/`가 루트에 들어와도 설계 문서와 섞이지 않는다. 기존 프로젝트(v13-mid 버전폴더 flat, mind2action 루트 flat 등)는 **이사하지 않는다** — 현 위치 유지. 이 골격은 착수 스킬(`/devinit`, 판단 6번)이 자동으로 깐다.
>
> 표준 폴더 골격:
> ```
> _dev/{프로젝트}/
>   docs/      ← 설계 3계층 (PLAN.md · TASKS.md · SPEC-{주제}.md) + DECISIONS.md
>   server/    ← 풀스택 백엔드 (API·DB·웹소켓). 단일 앱이면 생략
>   client/    ← 프런트엔드. 단일 앱이면 코드가 루트
>   README.md
> ```
>
> 5/14 갱신: AI 리터러시 v06 / DMZ v5 — PLAN·TASKS도 코드 폴더에 통합(볼트 다른 폴더에 흩어 두면 stale 위험 ↑). 통일부 회의록·답사 정리 같은 비코드 문서는 `current_projects/`에 유지.

### 규칙

1. **선문후코**: 변경 요청 → SPEC 먼저 갱신 → 코드 작성 → TASKS 체크 + 빌드. 탐색적 작업(CSS 미세 조정, 밸런스 값 튜닝)은 코드 확정 즉시 같은 턴에서 SPEC 반영. 다음 턴으로 미루지 않는다. (세션313 교훈: 코드 먼저 쓰고 SPEC 나중에 하면 세션 전환 시 누락)
2. **계획 노트 체크리스트는 완료 즉시 체크.** "나중에" 없다
3. **방향 전환 시 계획 노트에 즉시 반영.** 전환 이유 + 새 방향 기록. 이전 계획은 취소선
4. **세션 복귀 시 진행 작업 노트를 먼저 읽는다.** 세션 로그가 아니다
5. **에이전트 위임 시: 진행 작업 노트 + SPEC만으로 충분해야 한다.** 부족하면 노트를 보강한 뒤 위임
6. **빌드 완료 시 빌드 기록(바이트·해시·날짜·요약)을 해당 프로젝트 TASKS에 append.** 별도 통합 devlog는 두지 않는다 (6/14 은퇴 — 기존 `devlog.md`는 `_dev/_archive/`로 보존). 계획·진행 두 노트가 동기화
7. **버전 분기 시 옛 루트를 동결 처리한다.** 새 버전 폴더(`vXX/`)를 만들면, 옛 루트 PLAN 상단에 "현행 진입점 → `vXX/`" 포인터를 박고 옛 TASKS는 "동결" 마킹(미체크 항목이 미완료로 오인되지 않게). 빌드 기록을 TASKS에 일원화(규칙 6)했으므로, 현행 TASKS가 어디인지 불명확하면 빌드 기록 자체가 길을 잃는다. 세션316 "버전 분기 시 연결 값 교체"(storageKey·version·배포경로)의 노트판 짝. (6/14 신설)
8. **QA/외부 핸드오프 시 비개발자용 변경 요약을 남긴다.** 빌드를 QA(오공 등)나 피터공 외 전달할 때, "무엇이 바뀌었나"를 기술 용어 없이 서술형으로 정리해 해당 프로젝트 `PLAN.md`에 "인계 내역" 섹션으로 append. **매 커밋마다가 아니라 핸드오프 순간에만** — 통합 devlog(규칙 6, 6/14 은퇴)와 같은 실패를 반복하지 않는다. TASKS의 기술 기록(파일·해시·커밋)과는 다른 층: TASKS=개발자용 "무엇을 고쳤는가", 이 요약=비개발자용 "뭐가 달라 보이는가". AI리터러시의 "vXX 인계" 목록이 선례. (7/2 신설)

### 트리거 — 언제 이 규칙이 작동하는가

| 이벤트 | 아리공 행동 |
|--------|-----------|
| 개발 작업 착수 | 3계층 노트 존재 확인. 없으면 생성 먼저 |
| 코드 변경 완료 | 진행 노트 체크리스트 갱신 + SPEC 동기화 확인 |
| 방향 전환 (UI 변경, 포맷 변경 등) | 계획 노트 즉시 업데이트 |
| 버전 분기 (`vXX/` 폴더 신설) | 옛 루트 PLAN에 "현행 진입점 → vXX/" 포인터 + 옛 TASKS 동결 마킹 |
| 빌드/배포 완료 | 해당 프로젝트 TASKS에 빌드 기록(바이트·해시·날짜·요약) append |
| QA/외부 핸드오프 (빌드를 오공 등에게 넘길 때) | 해당 프로젝트 PLAN.md에 비개발자용 변경 요약("인계 내역") append |
| 세션 복귀 (/recall, /goodmorning) | 진행 노트 읽기 → `ls -lt _dev/{프로젝트}/`로 파일 시스템 교차 확인 |
| 에이전트 위임 전 | 진행 노트 + SPEC 읽어서 자급자족 가능한지 확인 |

### 프로젝트별 엔트리 포인트

각 프로젝트 폴더에 개발 계획 노트가 "지금 어디를 봐야 하는가"의 엔트리 포인트 역할을 한다. 돌아왔을 때 이 노트 하나만 읽으면 현재 상태를 파악할 수 있어야 한다.

---

## 빌드 전 체크 (필수)

프로토타입/앱을 만들기 전에 반드시 확인한다. 에이전트도 이 체크를 통과해야 작업을 시작한다.

1. **SPEC.md가 있는가?** — 없으면 코드를 쓰지 않는다. 에이전트 전달 가능한 디테일한 문서가 먼저 (세션142 교훈: SPEC 없이 4회 iterate → 전부 실패)
2. **콘텐츠(무엇을, 왜)가 확정되었는가?** — 확정 전에 메카닉/UI 설계에 들어가지 않는다. 콘텐츠가 먼저, 메카닉이 나중 (세션142 교훈: 메카닉에 매몰 → "콘텐츠에 힘이 없다")
3. **선택의 동기가 있는가?** — 플레이어가 왜 이걸 선택해야 하는지. "버튼이 있으니까 누른다"는 동기가 아님
4. **읽는 건가 하는 건가?** — 서술형 텍스트를 스크롤하는 구조면 게임이 아니라 문서
5. **레슨을 주고 있는가?** — 마지막에 교훈/메시지를 전달하면 실패. 보여주기만 하고 해석은 플레이어에게
6. **이 구조에서 정답이 생기는가?** — 모든 선택지 중 하나가 명백히 "올바른 선택"이면 퀴즈가 된다. 트레이드오프가 있는가? (세션158 교훈: ABC 모델 7개 매핑 후 폐기. 구조를 채우기 전에 1개만 체험 가능한 형태로 먼저 만들어서 확인)

이 6개 중 하나라도 걸리면 설계를 수정한 뒤 코드를 쓴다.

## 에이전트 프로토타입 완료 시 검증 (필수)

에이전트가 프로토타입을 만들면 전달 전에 반드시:
1. 모든 선택 가능한 경로(캐릭터, 분기)의 첫 스텝까지 동작 확인
2. 안 되는 경로가 있으면 수정 후 전달
3. DOMContentLoaded 초기화 코드 포함 확인

## 빌드·변경 후 검증·확인 흐름 (위임의 완성) — 26.0622

위임은 요청자(피터공) 확인까지가 완성이다. 아리공 자가검증(CDP·린터)은 그 확인을 **돕는 전처리**지 대체가 아니다. 자가검증으로 "PASS, 완료"라 닫지 않는다. 완료 선언권은 피터공에게.

1. **선문후코로 점검 범위를 미리 정한다** — SPEC에 "무엇을 만들지"와 함께 "검증 항목"을 적는다: (a) 아리공 자가점검분 (b) 피터공 확인분. 점검 범위 = SPEC에 약속한 만큼. 무한 검증으로 토큰을 태우지 않는다.
2. **자가점검 (하기로 한 그 정도)** — 런타임 예외 0 · 데이터 정합 · 회귀 · 텍스트 린터 · 이미지 로드 등 기계 판정 가능분을 SPEC 범위까지. CDP 자가검증은 여기까지가 역할.
3. **확인 포인트 제시 + 빌드 보여주기** — 피터공에게 두 묶음으로:
   - `[아리공이 점검함]` 예외 0 · 린터 통과 · 회귀 OK (SPEC 약속분 다 했음)
   - `[피터공이 확인하실 것]` 작동 화면의 상황 · 콘텐츠 눈높이와 톤 · 새 화면/인터랙션 + 라이브 URL
4. **완료는 피터공 확인 후** — 단, **커밋은 아니다(26.0812 정정)**. 자가검증(2번)을 통과한 세션 작업분은 **묻지 않고 그 자리에서 커밋한다.** 커밋은 완료 선언이 아니라 돌아갈 지점 확보라서, 확인을 기다리는 동안 로컬에만 두면 그게 위험이다. 피터공 확인이 필요한 것은 **완료 선언과 배포**다. "보라 했는데 안 보고 진행되면 피터공 선택"이되, 아리공이 보여줄 것을 건너뛰는 것만은 금지.
   - **좁게 스테이징하고 남긴 게 있으면 건수를 보고한다** — `git -C <repo> add <파일들>`로 파일 지정(`-A` 금지), push 전 `git show --stat HEAD`로 담긴 목록 눈으로 확인, 그리고 "미커밋 N건 남겼습니다(사유)". 침묵은 「없다」로 읽힌다.
   - **배경**: 「커밋은 요청 시에만」 + 「세션 작업분만」이 교차점에서 서로를 막아 `_dev`에 49건이 두 달 쌓였다. 각 규칙은 자기 자리에서 옳았고, 합치니 **누구도 올리지 않는 파일**이라는 범주가 생겼다. → [[요청.26.0812.0626-dev리포두달미커밋]]

배경: [[시스템 점검 — 26.0622 검토는 돌아가나만 보고 맞나는 안 봤다]]. 자가검증이 보여주기를 대체해 "피터공 모르는 사이 진행된 오류"가 쌓인 진단의 처방. 메모리: feedback_delegation_requires_requester_confirmation.

## 코드 컨벤션

- **단일 HTML 파일**: 한 파일에 HTML + CSS + JS 전부
- **바닐라 JS**: 프레임워크/라이브러리 없음
- **디자인**: 흰 배경, 미니멀, 블랙&화이트 기본. 탁한 색 금지.
- **한국어 우선**: UI 텍스트, 주석 모두 한국어

## 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v3.0 | 2026-03-10 | 초기 버전 (정예공 원본) |
| v3.1 | 2026-03-12 | 정예공 수정본 3건 반영, DATA-SPEC/CSV 생성, 4/4 올클리어 팝업 |
| v3.2 | 2026-03-12 | 게임코드 1953→DMZ (대소문자 무관+디엠지), 통일부+놀공 로고, delivery-screen 봉투 애니메이션 |
| v4.0-prep | 2026-04-29 | 정예공 새 빌드(4/28) 베이스 교체. `_dev/DMZ_v4/` 신설. 두 빌드 fork(mobile/offline) 설계. photos 영문화 47개. 개발 문서 7종. v3.2는 `_dev/DMZ/`에 레거시 보존. |
| v4.1-seq | 2026-04-30 | 세 번째 빌드 sequential 추가 — 자료 순차 잠금 + 정답 자료 라벨 강조. `shared/index_sequential.html` 별도 베이스. SPEC-sequential.md 신설. v4 작업 완성·푸시 |
| v5.0 | 2026-05-14 | v5 분기 — v4 통째 복사 (`_dev/DMZ_v5/`). storageKey prefix `dmz_v5_*`로 분리. docs v5 정정 + PLAN/TASKS 코드 폴더 통합 |
| v5.1-data | 2026-05-14 | STORIES 데이터 외부화 — `data/topics/*.yaml` 6개 + archivist_types.yaml. 두 베이스 HTML `// __STORIES_AND_TYPES__` placeholder. `build_stories_json.py` 신설. cat04 6 스토리 yaml 채움(s0401·s0403·s0404·s0405·s0406·s0407) + 사진 일괄 카피 |
| v5.2-pickone | 2026-05-15 | 네 번째 빌드 `pickone/` 신설 — sequential 룰 + 첫 자료 슬롯 가변. `data/first_source.csv` 24행(편집 가능). `shared/index_pickone.html` + `scripts/build_pickone.sh`. getUnlockOrder 사이클 알고리즘. LS_PREFIX `dmz_v5_p_`. SPEC-pickone.md 신설 |
| v5.3-design | 2026-05-15 | 비주얼 디자인 v1 도입(pickone 파일럿) — 통일부 디자이너 샘플 9장 적용. `assets/{icons,fonts,images}/` 신설(아이콘 9개·Paperlogy 3 weight·SD카드 타이틀). SPEC-ui-design.md 신설(컬러 토큰 6 카테고리·자료 type↔icon 매핑·화면별 컴포넌트). 로그인 SD카드·튜토리얼 핑크/cyan·카테고리 폴더격자·스토리 폴더격자·자료 리스트 핑크 카드+이미지 아이콘·자료 본문 모달 핑크 헤더·빈칸 cyan. 게임 코드 검증 임시 OFF. **5/15 결정: pickone canonical — mobile/offline/sequential 동결, 앞으로 pickone만 계속 업데이트** |
| v5.4-design2 | 2026-05-16 | pickone 디자인 v2 — 마닐라 폴더 패턴(좌상 사선 탭 + 뒤 흰 layer + 폴더 안 글씨) + 색 토큰 SVG 추출 정정 + 226·240 BG cat-color + 240 Z 위계(cat-tab 전체 폭 띠 + sheet + story-tab 우측 솟음) + drop shadow off + SD 카드 진입 애니메이션. 비교 빌드 pickone-v1/ 신설 |
| v5.5-design3 | 2026-05-16 | 세션361 — 화면 명칭 정본화(타이틀·튜토리얼·주제선택·스토리선택·자료선택·자료본문·보관소·스토리완료·결과) + 좌하단 라벨. SPEC-screens.md 신설(화면별 세부 명세, 자료선택 § 우선). PNG 자산 통합(상단바 로고 6장 cat01·02·03·04·06+white, 프로필, 자료 status 3종). 자료선택 game-header 폐기 + 주제 띠→스토리선택. 폰트 weight 600 일괄. 폴더 aspect-ratio 1.625 + 레이블 외부 + 뒷장 마닐라 모양(folder-back div) + 색(주제선택 흰색/스토리선택 cat-color 55% 라이트). 마닐라 mirror·좌상 탭 결(clip-path polygon). 두 화면 헤더 layout 통일 + 고정 높이(64/82). 자료 카드 박스 160 + status 아이콘 mask |

## 현재 게임 상태 (v3.2)

- **게임코드**: `DMZ` (대소문자 무관, `디엠지`도 허용)
- **로그인 화면**: 타이틀 + 이름/코드 입력 + 하단 통일부·놀공 로고
- **delivery-screen**: 서류봉투 우편 발송 시퀀스 (카드삽입→봉투닫힘→통일부 도장→날아감)
- **캐릭터 4명**: 각각 4개 자료 + 4개 빈칸 = 총 16자료, 12빈칸 (일부 공유)
- **GitHub Pages**: https://mice3nyc.github.io/ari_gitrepo_1/DMZ/

## 아리온에게

너는 이 리포의 코드를 읽고 수정할 수 있어. 피터공이 모바일에서 작업을 지시하면 코드를 수정하고 커밋해줘.
아리공(로컬)이 PM으로서 큰 방향을 잡고, 너(아리온)는 실행을 담당한다. 함께 잘 해보자!

**자료 찾는 곳:**
- 코드: `DMZ/index.html` (단일 파일)
- UI 이름: `DMZ/docs/UI-MAP.md` — 화면/요소 이름 통일. 소통할 때 이 이름 사용
- 데이터: `DMZ/docs/DATA-SPEC.md` — 이미지 네이밍, CSV 포맷, 빈칸 규칙
- 인수인계: `DMZ/docs/HANDOFF.md` — 아리공↔아리온 소통 채널. push 전에 여기 기록
- 구조도: `DMZ/docs/ARCHITECTURE.md` — mermaid 다이어그램
- 정답표: `DMZ/dmz_blanks.csv` — source of truth. 코드와 항상 일치해야 함
- 선택지: `DMZ/dmz_choices.csv`
- 이미지/로고: `DMZ/media/`
