---
created: 2026-05-14
tags:
  - 요청
  - DMZ
  - 통일부
  - 개발
author: 아리공
---
### 요청.26.0514.1120 — DMZ v5 코드 리뷰

> "AI 리터러시에서 개발과 관리 방식을 알고 있지? 우리가 어떻게 기획부터 개발과 검수를 관리하고 코드를 어떤 방식으로 했는지. 그렇지, 이번부터는 그렇게 진행해 보자. 먼저, 일단 그 관점에서 현재 코드 리뷰가 필요하다." — 피터공, 5/14 11:20

---

#### 진입 박스

- **출발점**: DMZ v5 분기 완료(`b2b5d95`). 새 변경 진입 전 현재 코드·관리 체계 진단.
- **현재 위치**: 리뷰 1차 + 정리 묶음 완료 (커밋 `7b91e51`).
- **다음 한 동작**: 새 변경 사양 수신 → PLAN 신설 → SPEC → 코드.
- **가장 큰 발견**: 코드 자체는 v5 분기 완료, 그러나 **docs 7종 헤더가 모두 "DMZ v4"로 stale**. HANDOFF는 sequential 빌드 신설(4/30) 이후 한 줄도 갱신 안 됨. 묶음 정리로 해소.

#### 리뷰 관점 — AI 리터러시 5결

**1. 3계층 노트 + 요청 노트** — 부분 OK / 보강 필요

- v4 PLAN ✅ ([[26.0429 DMZ v4 개발 계획]]) / TASKS ✅ (2건, 4/29·4/30) / SPEC ✅ (코드 폴더 안)
- **v5 PLAN ❌** — 신설 필요. v5에서 들어갈 변경 사양 받으면 작성
- v5 TASKS = [[26.0514 DMZ v5 분기 진행 작업]] (분기 작업 노트만, 새 Phase 진입 시 별도 진행 노트 또는 같은 노트 Phase 2 추가)
- 위치 split — PLAN/TASKS는 `current_projects/통일부 DMZ/`, SPEC은 `_dev/DMZ_v5/docs/`. AI 리터러시 v06 패턴(`v06/SPEC.md`)과 일관. 다만 PLAN/TASKS도 코드 폴더 안에 둘지 결정 가능

**2. 선문후코 (SPEC ↔ 코드)** — 차이 3건

- SPEC.md L59 unlock 키 명세 `dmz_v5_offline_unlocks` ↔ 실제 코드 `LS_PREFIX + 'unlocks'` (= `dmz_v5_o_unlocks`). 동작 정상, 문서 stale
- SPEC-sequential.md `isSourceUnlocked(storyId, sourceId)` ↔ 실제 `isSourceUnlocked(story, sourceId)` (객체 전달). 백도에 문서만 주면 시그니처 혼선
- CODE-FORK-POINTS.md 라인 번호 stale — renderSource L517→실제 L593, submitAnswer L2233→L2384 등 (코드 추가로 라인 밀림)

**3. 버전 분기 위생** — 위험

- 코드 자체 v5 grep 0건 잔존, 정정 ✅
- **docs 헤더 7개 모두 `# DMZ v4 — XXX`** — sed가 `DMZ_v4` / `dmz_v4_`만 잡고 `DMZ v4` (공백) 미정정. 본문에 `v4 신규` / `v3.2 폐기` 등 v4 시점 표현 다수
- _dev/CLAUDE.md 리포 가이드도 `DMZ_v4/` 폴더 구조만 명시, v5 미반영
- HANDOFF.md L46: localStorage 키 검토 메모가 `dmz_diary_v2`로 stale — 실제 코드는 이미 `dmz_v5_*` 완료. 아리온이 보면 불필요한 작업 시도 가능

**4. 데이터 분리** — OK / 보강 필요

- csv/manifest 분리 ✅, build_sequential.sh가 CSV → JS BLANK_SOURCE_LOOKUP 자동 주입 round-trip ✅
- DATA-SPEC.md 명세 컬럼명 `story_id, blank_key` ↔ 실제 CSV 헤더 `episode_id, blank_id` — 빌드 통과(스크립트는 실제 컬럼명 사용), 문서가 stale
- `answer_from` 비어 있는 3행(`s0104_B`, `s0301_A`, `s0306_C`) 처리 경로 미문서화

**5. Phase 단위 + 검증** — OK

- build.sh: sed 한 줄 분기 + grep 검증. mobile 163,496B / offline 163,495B (1바이트 차이 정상)
- build_sequential.sh: 매핑 71개 주입 + node JS syntax 검증 포함
- 메인이 방금 v5 빌드 통과 확인
- `twitter` type — renderSource switch에 case 있으나 STORIES 사용 0건. dead branch (정도가 경미)

#### 처리 결과

##### 진입 전 반드시 정정 (4건) ✅

1. ✅ docs 7종 헤더 v4→v5 일괄 + 본문 시점 표현 정정 (sed + 개별 Edit)
2. ✅ HANDOFF.md 전면 재작성 — 3 빌드 비교 표, sequential 섹션, LS 키 정책, stale 메모 처리
3. ✅ SPEC.md L59 unlock 키 문자열 정정 (`dmz_v5_o_unlocks`로)
4. ✅ SPEC-sequential.md isSourceUnlocked 시그니처 정정 (story 객체)

##### 진입 전 결정 (2건) ✅

5. **PLAN/TASKS 위치** — `_dev/DMZ_v5/docs/` 코드 폴더 통합 채택 (AI 리터러시 v06 패턴). 볼트 진행 노트 4종 + 요청 노트 2종 이동, _init·세션 로그 옛 링크 정정
6. **v5 PLAN 신설 자리** — 새 변경 사양 받은 후 진입

##### 진입하며 정리 (전부 처리) ✅

- ✅ DATA-SPEC 컬럼명 정정 (`episode_id`, `blank_id`) + `answer_from` 빈 3행 처리 명시
- ✅ CODE-FORK-POINTS 라인 번호 재정찰 (renderSource 517→543, submitAnswer 2233→2273 등)
- ✅ UI-MAP sequential 컴포넌트 5종 추가 (`.source-card.locked` 외 4종)
- ✅ `_dev/CLAUDE.md` 변경 이력 + 폴더 구조 v5 반영 + 위치 룰 갱신 (PLAN·TASKS 코드 폴더 통합)
- ✅ DMZ_v4/mobile 누락 CSS 패치 같이 반영 (v4 일관성 회복)
- ⏳ `twitter` type dead branch → SPEC 미해결 섹션에 명시. cat04~06 데이터 확정 시 결정

##### 커밋

- `3a3435c` — v4 미커밋 CSS 패치 (`.bd-hidden .detail-close button`)
- `b2b5d95` — DMZ v5 분기 (v4 통째 복사 + storageKey 교체)
- `7b91e51` — v5 docs 정리 + PLAN/TASKS 코드 폴더 통합 + v4 mobile 패치 일관화

#### 미해결 / 다음 회기

- [ ] v5에서 들어갈 새 변경 사양 수신 → `_dev/DMZ_v5/docs/PLAN.md` 신설 → SPEC 갱신 → 코드 진입 (선문후코)
- [ ] `twitter` type dead branch — cat04~06 데이터 확정 시 사용/제거 결정
- [ ] v4/scripts/build.sh 재실행 여부 — 본 커밋에 mobile/index.html 패치만 직접 반영, 정합 위해 baseline 빌드 1회 권장 (현재 5/26 베타용으로 보존 우선)

→ [[TASKS|v5 TASKS]]
