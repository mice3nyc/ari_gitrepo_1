words/
.DS_Store
beingfaust_values/references/

# 독립 저장소 (자체 remote — 부모 repo에 넣지 않음)
mind2action/
mind2action-home/
# 동현공 2025 코드베이스 clone (nolgong-dev org 리포, 위메이크 성수 2026 참조용).
# 자체 .git이 있어 여기 커밋하면 gitlink 껍데기만 들어가고, 놀공 org 코드가 개인 리포로 오배치된다.
we-make-seongsu-back-2025/
we-make-seongsu-front-2025/

# 빌드 산물 / 의존성
*.app/
**/node_modules/
**/__pycache__/
# AI리터러시 변종 빌드 산출 폴더 (생성물 — 동현공 전달은 폴더 직접 전달, git 미포함)
ai-literacy-delegation-boundary/v13-mid/builds/
ai-literacy-delegation-boundary/v13-elem/builds/
# v14도 같은 규칙. 이 줄이 없어서 v14/builds/ 408개(webp 396)가 두 달간 「미커밋 511건」으로 보였다 (26.0812)
ai-literacy-delegation-boundary/v14/builds/

# 런타임 로그·상태
# vault_write.py 덮어쓰기 로그 + 스킬 완료 마커(memento_done_*). 기계가 만드는 상태라 커밋 대상이 아니다.
scripts/logs/
context-hop-bar/logs/
context-hop-bar/data/
context-hop-bar/*.db
context-hop-bar/*.jsonl
tm-bar/data/

# 마이그레이션 백업 스냅샷
*.before-v*-migration
# 편집 직전 스냅샷 (yaml 손보기 전 사본 — 위와 같은 범주인데 접미가 자유롭다)
*.yaml.before-*
# 편집 직전 사본 (tm.sh.bak_claim, session_num.sh.bak_lockunify 같은 것)
*.bak_*

# 스크래치 (작업 중 임시 산출 — 참조하는 문서가 없는 것)
todoy-bar/scratch_*
